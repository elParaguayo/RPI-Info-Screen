# Set the folders to be scanned
[Albums]
# Separate folders by a comma.
# List must be within square brackets
# list can span multiple lines as long as following lines are indented e.g.:
# folders=/photo/folder/,
#         /another/photo/folder/
folders=/photo/folder/,
        /another/photo/folder/

# Do recursive search of folders?
# To be implemented
recursive=False
